
<h1>Отчет по лабораторной работе 1<h1>

<h1>Основные этапы анализа<h1>

1. Загрузка и первоначальный осмотр данных
   
Проект начинается с загрузки CSV-файла `````ncr_ride_bookings.csv````` с обработкой возможных ошибок:

`````
try:
    df = pd.read_csv("ncr_ride_bookings.csv")
    print("Файл загружен успешно!")
    print(f"Размер данных: {df.shape[0]} строк, {df.shape[1]} столбцов")
except FileNotFoundError:
    print("Файл 'ncr_ride_bookings.csv' не найден")
    exit()
`````  
Использование блока try-except обеспечивает стабильность работы программы при отсутствии файла или других ошибках ввода-вывода.

<br>2. Исследование структуры данных
   
После успешной загрузки выполняется анализ структуры данных:

`````
print(f"Реальные названия столбцов ({len(df.columns)}):")
for i, col in enumerate(df.columns, 1):
    print(f"  {i}. {col}")
`````
Автоматическое определение реальных названий столбцов делает код универсальным и не зависящим от конкретного формата исходных данных.

<br>3. Анализ качества данных
   
Проверка пропущенных значений и статистический анализ:

`````
missing = df.isnull().sum()
for col, count in missing.items():
    if count > 0:
        print(f"  {col}: {count} пропусков")
`````
Детальный анализ пропущенных значений помогает оценить качество данных и необходимость их предобработки.

<br>4. Интеллектуальная фильтрация данных

 Проект реализует умный поиск столбцов по ключевым словам:

 `````
status_columns = [col for col in df.columns if 'status' in col.lower() or 'Status' in col]
vehicle_columns = [col for col in df.columns if 'vehicle' in col.lower() or 'Vehicle' in col]
date_columns = [col for col in df.columns if 'date' in col.lower() or 'time' in col.lower()]
 `````
Такой подход позволяет работать с различными форматами данных без необходимости ручного указания названий столбцов.

<br>5. Фильтрация по статусам

Автоматическое определение и фильтрация отмененных бронирований:

 `````
cancel_statuses = [s for s in unique_statuses if 'cancel' in str(s).lower()]
if cancel_statuses:
    cancel_status = cancel_statuses[0]
    cancelled = df[df[status_col] == cancel_status]
 `````
Алгоритм самостоятельно находит статусы, содержащие слово "cancel", что делает анализ гибким к различным формулировкам в данных.
<br>
<br>
<br>
<br>
<br>
<h1>Отчет по лабораторной работе 2<h1>

<h1>Компоновка интерфейса<h1>

Основные элементы дизайна:

1. Главное окно (800x600 пикселей) - оптимальный размер для отображения данных
2. Иерархическая структура:

-  Заголовок приложения
-  Панель управления с кнопками
-  Область вывода результатов с прокруткой
-  Строка состояния
3. Визуальные компоненты:
   <br>
`````
main_frame = ttk.Frame(self.root, padding="10")
title_label = ttk.Label(main_frame, text="Анализ данных Uber", 
                       font=("Arial", 16, "bold"))
self.output_area = scrolledtext.ScrolledText(main_frame, width=80, height=30,
                                            font=("Consolas", 9))
`````
  <br>
  Принципы дизайна
  
 <br>
   1.  Последовательность состояний кнопок:
   
`````
self.overview_btn.config(state="normal")
self.overview_btn.config(state="disabled")
`````
<br>
  2.  аптивная компоновка:
  
`````
main_frame.columnconfigure(0, weight=1)
main_frame.rowconfigure(2, weight=1)
`````
<br>
<br>
<br>
<br>
<h1>Отчет по лабораторной работе 3<h1>

Новый функционал:<br>

Анализ пропущенных значений:

-  Подсчет пропусков по каждому столбцу  
-  Расчет процента пропущенных значений  
-  Анализ строк с пропущенными данными

<br>Анализ уникальных значений:  
   
-  Автоматическое определение категориальных столбцов
-  Частотный анализ для топ-10 значений
-  Специальный анализ для Booking Status и Vehicle Type

Используемые библиотеки:  
`````
import pandas as pd
import numpy as np
import tkinter as tk
from tkinter import ttk, scrolledtext, messagebox
import threading
`````
Добавленный функционал:

<br>1. Анализ пропущенных значений:
`````  
def analyze_missing_values(self):
    """Анализ пропущенных значений во всех столбцах"""
    if self.df is None:
        messagebox.showwarning("Предупреждение", "Сначала загрузите данные")
        return

    self.clear_output()
    self.update_output("=" * 70)
    self.update_output("АНАЛИЗ ПРОПУЩЕННЫХ ЗНАЧЕНИЙ")
    self.update_output("=" * 70)

    missing_data = self.df.isnull().sum()
    total_cells = np.prod(self.df.shape)
    total_missing = missing_data.sum()

    self.update_output(f"\nОбщее количество пропущенных значений: {total_missing}")
    self.update_output(f"Общее количество ячеек в данных: {total_cells}")
    self.update_output(f"Процент пропущенных значений: {(total_missing / total_cells) * 100:.2f}%")

    self.update_output("\nДетальный анализ по столбцам:")
    self.update_output("-" * 70)
    self.update_output(f"{'Столбец':<30} {'Пропущено':<12} {'Всего':<10} {'% пропусков':<15}")
    self.update_output("-" * 70)

    for col in self.df.columns:
        missing_count = missing_data[col]
        total_rows = len(self.df)
        missing_percent = (missing_count / total_rows) * 100
        self.update_output(f"{col:<30} {missing_count:<12} {total_rows:<10} {missing_percent:<15.2f}")
`````
<br>2. Анализ уникальных значений:
`````
def analyze_unique_values(self):
    """Анализ уникальных значений в категориальных столбцах"""
    if self.df is None:
        messagebox.showwarning("Предупреждение", "Сначала загрузите данные")
        return

    self.clear_output()
    self.update_output("=" * 70)
    self.update_output("АНАЛИЗ УНИКАЛЬНЫХ ЗНАЧЕНИЙ В КАТЕГОРИАЛЬНЫХ СТОЛБЦАХ")
    self.update_output("=" * 70)

    # Поиск категориальных столбцов
    categorical_columns = []
    for col in self.df.columns:
        if self.df[col].dtype == 'object' or self.df[col].nunique() < 50:
            categorical_columns.append(col)

    self.update_output(f"\nНайдено категориальных столбцов: {len(categorical_columns)}")

    for col in categorical_columns:
        self.update_output(f"\n{'-' * 70}")
        self.update_output(f"СТОЛБЕЦ: {col}")
        self.update_output(f"{'-' * 70}")

        unique_values = self.df[col].unique()
        unique_count = self.df[col].nunique()
        missing_count = self.df[col].isnull().sum()

        self.update_output(f"Количество уникальных значений: {unique_count}")
        self.update_output(f"Пропущенных значений: {missing_count}")

        if unique_count <= 20:
            self.update_output(f"Уникальные значения: {list(unique_values)}")
        else:
            self.update_output(f"Первые 20 уникальных значений: {list(unique_values[:20])}")

        # Частотный анализ для топ-10 значений
        if unique_count > 0:
            self.update_output(f"\nТоп-10 самых частых значений:")
            value_counts = self.df[col].value_counts().head(10)
            for value, count in value_counts.items():
                percentage = (count / len(self.df)) * 100
                self.update_output(f"  {value}: {count} ({percentage:.2f}%)")
`````
<br>
<br>
<br>
<br>

<h1> ИИ для расчета стоимости поездки Uber <h1>

Архитектура и принцип работы приложения:  
<br>
1. Основные компоненты системы
Класс UberPricePredictor - ядро машинного обучения: 

`````
class UberPricePredictor:
    def __init__(self):
        self.df = None          # DataFrame с данными
        self.model = None       # Обученная модель ML
        self.scaler = StandardScaler()  # Нормализатор данных
        self.label_encoders = {} # Кодировщики категориальных данных
`````
<br>Класс ModernUberPredictorGUI - пользовательский интерфейс:
`````
class ModernUberPredictorGUI:
    def __init__(self):
        self.root = tk.Tk()     # Главное окно приложения
        self.predictor = UberPricePredictor()  # ML компонент
`````
<br>2. Рабочий процесс приложения

Этап 1: Загрузка и подготовка данных
`````
def load_data(self, file_path):
    self.df = pd.read_csv(file_path)
    self.df = self.df.dropna(subset=['Booking Value'])
`````
<br>Создание дополнительных признаков:
`````
def create_additional_features(self):
    # Временные факторы
    self.df['hour'] = self.df['booking_datetime'].dt.hour
    self.df['is_peak_hours'] = ((self.df['hour'] >= 7) & (self.df['hour'] <= 9)).astype(int)
    
    # Погодные и экономические факторы
    self.df['temperature'] = np.random.normal(25, 8, len(self.df))
    self.df['demand_surge'] = np.random.lognormal(0, 0.3, len(self.df))
`````
<br>Этап 2: Обучение модели машинного обучения
`````
def train_model(self):
    X, y = self.prepare_features_for_ml()
    X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)
    
    self.model = RandomForestRegressor(n_estimators=100, random_state=42)
    self.model.fit(X_train, y_train)
    
    # Оценка точности
    y_pred = self.model.predict(X_test)
    mae = mean_absolute_error(y_test, y_pred)
    r2 = r2_score(y_test, y_pred)
`````
<br>Этап 3: Предсказание стоимости
`````
def predict_price(self, input_features):
    input_df = pd.DataFrame([input_features])
    input_scaled = self.scaler.transform(input_df)
    return self.model.predict(input_scaled)[0]
`````

<br>3. Дизайн пользовательского интерфейса

Цветовая схема:
-  Основной фон: #2c3e50 (темно-синий)
-  Акцентные цвета: #3498db (синий), #e74c3c (красный)
-  Текст: белый и светло-серый

<br> Компоновка элементов:
`````
# Левая панель - управление и калькулятор
left_panel = tk.Frame(content_frame, bg='#2c3e50', width=400)

# Правая панель - визуализация и логи
right_panel = tk.Frame(content_frame, bg='#2c3e50')

# Вкладки для различной информации
notebook = ttk.Notebook(parent)
notebook.add(log_frame, text='📊 Логи')
notebook.add(stats_frame, text='📈 Статистика')
notebook.add(graph_frame, text='📉 Графики')
`````
<br>4. Ключевые особенности реализации

Обработка данных:
-  Обработка пропущенных значений
- Кодирование категориальных переменных
-  Нормализация числовых данных
<br>Машинное обучение:
-  Алгоритм: Random Forest (ансамбль деревьев решений)
-  Валидация: разделение на обучающую/тестовую выборки
-  Метрики: MAE и R² для оценки точности
<br>
5. Используемые библиотеки

Для машинного обучения:
`````
import pandas as pd
import numpy as np
from sklearn.ensemble import RandomForestRegressor
from sklearn.preprocessing import StandardScaler, LabelEncoder
`````
<br>Для графического интерфейса:
`````
import tkinter as tk
from tkinter import ttk, scrolledtext
import matplotlib.pyplot as plt
from matplotlib.backends.backend_tkagg import FigureCanvasTkAgg
`````
<br>

6. Принцип работы ИИ

Random Forest алгоритм:
1. Создает множество деревьев решений на подвыборках данных
2. Каждое дерево обучается на случайном наборе признаков
3. Итоговое предсказание - среднее значение всех деревьев
4. Оценивает важность каждого признака для прогноза
   

